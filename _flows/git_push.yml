id: git_push
namespace: bottleneck

tasks:
  - id: push
    type: io.kestra.plugin.git.PushFlows
    url: https://github.com/maevabb/p10_monthly-wine-data-pipeline
    username: maevabb
    password:  '{{ secret("GITHUB_TOKEN") }}'
    branch: main
    sourceNamespace: "bottleneck"
    commitMessage: "Test github push flow"